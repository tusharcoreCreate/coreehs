# Practical Test – Angular 17 (Standalone) + Reactive Form Wizard + D3 Tree

## Run
```bash
npm install
npm start
```

If `ng` command is not found, install Angular CLI 17:
```bash
npm i -g @angular/cli@17
```

## Notes
- Multi-step wizard with Parent (FormWizard) + Child step components.
- Draft save bypasses validation (localStorage).
- Final submit enforces all validations.
- D3 tree renders the sample JSON using d3.
# coreehs
front-end practical
